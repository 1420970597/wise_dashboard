实际应用场景

  本地转发 (-L) 适用于：

  - Agent 访问 Dashboard 的数据库
  - Agent 访问 Dashboard 的内网服务
  - Agent 通过 Dashboard 作为跳板访问其他服务

  远程转发 (-R) 适用于：

  - Dashboard 需要 SSH 到 Agent（内网穿透）
  - Dashboard 访问 Agent 的本地服务
  - 将 Agent 的服务暴露到 Dashboard 上